using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectEvents : MonoBehaviour
{
    //https://www.youtube.com/watch?v=fA79neqH21s
    //Late in the video follow tutorial
    //This script is used to determine what object it is and what event should occur
    //Only work after you finish watching the rest of the series.
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
